{
    "name": "url-shortener",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev:backend": "bun run --cwd packages/backend dev",
        "build:frontend": "bun run --cwd packages/frontend build",
        "watch:frontend": "bun run --cwd packages/frontend watch",
        "dev": "bun run build:frontend && sh -c 'bun run watch:frontend & watcher=$!; trap \"kill $watcher 2>/dev/null || true\" EXIT; bun run dev:backend'"
    }
}